git status
git remote -v
git remote -u
git commit
npm install

Running Application
    -npm start
    -"contol+c" to stop
    -preview running application
    
    Display modes
        flex
        block
        inline
    Position Values
        fixed = top/left/bottom from edge of browser document
        relative = t/l/r/b relative to first parent that isn't inherited
        absolute
    Precedence
    2 rules
       1 by specificity
            1. style in tag
            2. id
            3. class
            4. tags
       2 then by last on page